import { DelimiterToTitlePipe } from './delimiter-to-title.pipe';

describe('DelimiterToTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new DelimiterToTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
